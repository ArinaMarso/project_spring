<Window x:Class="StoreG5G11.views.InstrumentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:StoreG5G11.src.modelViews"
        xmlns:entities="clr-namespace:StoreG5G11.models.ef.entities"
        mc:Ignorable="d"
        Title="Управление инструментами" Height="600" Width="900">

    <Window.DataContext>
        <local:InstrumentViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.7*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Список инструментов" 
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding Items}"
                     SelectedItem="{Binding SelectedItem}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#BDC3C7" BorderThickness="1" 
                                CornerRadius="5" Margin="0,2" Padding="10">
                            <StackPanel>
                                <TextBlock FontSize="16" FontWeight="Bold" 
                                           Text="{Binding Path=Name}"/>
                                <TextBlock Text="{Binding Path=Brand}" 
                                           Foreground="#2C3E50" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Path=InstrumentType}" 
                                           Foreground="#7F8C8D" Margin="0,5,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="{Binding Path=Price, StringFormat='Цена: {0:C}'}" 
                                               Foreground="#27AE60" FontWeight="Bold"
                                               Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Path=StockQuantity, StringFormat='На складе: {0} шт.'}" 
                                               Foreground="#3498DB"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Редактирование" 
                       FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <Border Grid.Row="1" BorderBrush="#BDC3C7" BorderThickness="1" 
                    CornerRadius="5" Padding="15" DataContext="{Binding SelectedItem}">
                <StackPanel>
                    <TextBlock Text="Название:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,0,10" Height="30"/>

                    <TextBlock Text="Бренд:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,0,10" Height="30"/>

                    <TextBlock Text="Тип инструмента:" FontWeight="SemiBold" Margin="0,0,0,5"/>

                    <ComboBox ItemsSource="{Binding InstrumentTypes}"
                              SelectedItem="{Binding InstrumentType}"
                              Margin="0,0,0,10" Height="30"/>

                    <TextBlock Text="Цена (руб):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,0,10" Height="30"/>

                    <TextBlock Text="Количество на складе:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding StockQuantity, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,0,10" Height="30"/>

                    <TextBlock Text="Описание:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" Height="80" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                        HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Добавить" Command="{Binding AddCommand}" 
                        Background="#3498DB" Foreground="White"
                        Width="100" Height="35" Margin="5"/>

                <Button Content="Удалить" Command="{Binding RemoveCommand}" 
                        Background="#E74C3C" Foreground="White"
                        Width="100" Height="35" Margin="5"/>

                <Button Content="Сохранить" Command="{Binding SaveCommand}" 
                        Background="#2ECC71" Foreground="White"
                        Width="100" Height="35" Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
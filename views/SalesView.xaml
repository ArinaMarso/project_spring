<Window x:Class="StoreG5G11.views.SalesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:StoreG5G11.src.modelViews"
        mc:Ignorable="d"
        Title="Продажа инструментов" Height="700" Width="1000">

    <Window.DataContext>
        <local:SaleViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Информация о продаже" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="10">
                    <TextBlock Text="Сотрудник:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Employees}"
                              SelectedValue="{Binding CurrentSale.EmployeeId}"
                              DisplayMemberPath="FullName"
                              SelectedValuePath="Id"
                              Height="30" Margin="0,0,0,10"/>

                    <TextBlock Text="Метод оплаты:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding PaymentMethods}"
                              SelectedItem="{Binding CurrentSale.PaymentMethod}"
                              Height="30" Margin="0,0,0,10"/>

                    <TextBlock Text="Дата:" FontWeight="Bold" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding CurrentSale.Date}"
                                Height="30" Margin="0,0,0,10"/>
                </StackPanel>

                <Border Grid.Column="1" Margin="10" BorderBrush="#3498DB" 
                        BorderThickness="2" CornerRadius="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="ИТОГО ПРОДАЖИ" 
                                   FontSize="18" FontWeight="Bold" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,15"/>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,10">
                            <TextBlock Text="Количество позиций:" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding CurrentSale.Orders.Count}" 
                                       Margin="10,0,0,0"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,10">
                            <TextBlock Text="Общая сумма:" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding CurrentSale.Total, StringFormat={}{0:C}}" 
                                       FontSize="20" FontWeight="Bold" Foreground="#E74C3C"
                                       Margin="10,0,0,0"/>
                        </StackPanel>

                        <Button Content="Завершить продажу" 
                                Command="{Binding CompleteSaleCommand}"
                                Background="#244a22" Foreground="White"
                                Height="40" FontSize="16" FontWeight="Bold"
                                Margin="0,15,0,0" Click="Button_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Добавить инструмент" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" 
                          ItemsSource="{Binding AvailableInstruments}"
                          SelectedItem="{Binding SelectedInstrument}"
                          DisplayMemberPath="Name"
                          Margin="10" Height="30"/>

                <TextBlock Grid.Column="1" Text="Количество:" 
                           VerticalAlignment="Center" Margin="10,0,5,0"/>

                <TextBox Grid.Column="2" 
                         Text="{Binding QuantityToAdd}"
                         Width="60" Height="30" Margin="5,0"/>

                <Button Grid.Column="3" Content="Добавить" 
                        Command="{Binding AddToSaleCommand}"
                        Background="#1a608f" Foreground="White"
                        Width="100" Height="30" Margin="10,0"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Товары в продаже">
            <DataGrid ItemsSource="{Binding CurrentSale.Orders}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Инструмент" 
                                        Binding="{Binding Instrument.Name}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Бренд" 
                                        Binding="{Binding Instrument.Brand}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Цена" 
                                        Binding="{Binding Price, StringFormat={}{0:C}}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="Количество" 
                                        Binding="{Binding Count}" 
                                        Width="80"/>
                    <DataGridTextColumn Header="Сумма" 
                                        Binding="{Binding Total, StringFormat={}{0:C}}" 
                                        Width="120"/>
                    <DataGridTemplateColumn Header="Действия" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" 
                                        Command="{Binding DataContext.RemoveFromSaleCommand, 
                                                 RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Background="#E74C3C" Foreground="White"
                                        Padding="5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>